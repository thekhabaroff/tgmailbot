# 🤖 Telegram Bot - Магазин цифровых товаров

> Автоматизированный Telegram-бот для продажи цифровых товаров (почтовых аккаунтов) с автоматической выдачей после оплаты

[![Python](https://img.shields.io/badge/Python-3.10+-blue.svg)](https://www.python.org/)
[![aiogram](https://img.shields.io/badge/aiogram-3.3.0-green.svg)](https://docs.aiogram.dev/)
[![License](https://img.shields.io/badge/License-Proprietary-red.svg)](LICENSE)

---

## 📋 Содержание

- [Возможности](#-возможности)
- [Быстрый старт](#-быстрый-старт)
- [Установка](#-установка)
- [Использование](#-использование)
- [Развертывание](#-развертывание)
- [Структура проекта](#-структура-проекта)
- [Поддержка](#-поддержка)

---

## ✨ Возможности

### 👤 Для пользователей
- 🛒 **Каталог товаров** с категориями и фильтрацией
- 💰 **Множество способов оплаты**: баланс, ЮКасса, Heleket, Telegram Stars
- 📦 **Автоматическая выдача** товаров после оплаты
- 💳 **Система баланса** с пополнением
- 👥 **Реферальная программа** (10% комиссия)
- 🔔 **Подписки на уведомления** о поступлении товара
- 📋 **История заказов** с возможностью повторной выгрузки

### ⚙️ Для администраторов
- 🎛️ **Админ-панель** для управления магазином
- 📊 **Статистика** по заказам и пользователям
- 📦 **Управление каталогом**: категории, товары, аккаунты
- 👥 **Управление пользователями**: блокировка, пополнение баланса
- 📢 **Рассылка** сообщений (массовая и индивидуальная)
- 📝 **Логирование** ошибок и событий
- 🔔 **Уведомления** о покупках и регистрациях

### 🔒 Безопасность
- ✅ Транзакции БД (ACID) для предотвращения двойной продажи
- ✅ Бронирование товаров на 15 минут
- ✅ Ограничение на количество неоплаченных заказов
- ✅ Суперпользователи назначаются только через `.env`, остальные через бота 
- ✅ Аккаунты не логируются в открытом виде

---

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/thekhabaroff/MailBot
cd mailbot
```

### 2. Создание виртуального окружения

**⚠️ ВАЖНО:** Бот должен запускаться через интерпретатор Python из виртуального окружения (venv).

```bash
# Создаем виртуальное окружение
python3 -m venv venv

# Активируем виртуальное окружение
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
# Убедитесь, что venv активирован (должна быть префикс (venv) в командной строке)
pip3 install --upgrade pip
pip3 install -r requirements.txt
```

### 3. Настройка окружения

Создайте файл `.env` в корне проекта:

```env
BOT_TOKEN=your_bot_token_from_botfather
DATABASE_URL=sqlite+aiosqlite:///./bot.db
ADMIN_IDS=123456789,987654321
SUPPORT_CHAT=@your_support_username
REFERRAL_COMMISSION=10
ORDER_RESERVATION_MINUTES=15
BROADCAST_THROTTLE=25
```

### 4. Запуск бота

**⚠️ ВАЖНО:** Запускайте бота через интерпретатор Python из venv:

```bash
# Убедитесь, что venv активирован
source venv/bin/activate

# Запуск бота через интерпретатор из venv
venv/bin/python3 main.py
```

Или если venv активирован:

```bash
python3 main.py
```

**Готово!** 🎉 Бот запущен и готов к работе.

---

## 📦 Установка

### Требования

- Python 3.10 или выше
- pip (менеджер пакетов Python)
- Токен бота от [@BotFather](https://t.me/BotFather)

### Пошаговая установка

1. **Установите Python 3.10+**

   Проверьте версию:
   ```bash
   python --version
   ```

2. **Создайте виртуальное окружение и установите зависимости**

   ```bash
   # Создаем виртуальное окружение
   python3 -m venv venv
   
   # Активируем виртуальное окружение
   source venv/bin/activate
   
   # Устанавливаем зависимости
   pip3 install --upgrade pip
   pip3 install -r requirements.txt
   ```

3. **Настройте базу данных**

   База данных создается автоматически при первом запуске. Для SQLite ничего дополнительно делать не нужно.

   Для PostgreSQL см. раздел [Настройка базы данных](#-настройка-базы-данных).

---

## ⚙️ Настройка

### Основные параметры

#### `.env` файл

```env
# Обязательные параметры
BOT_TOKEN=your_bot_token_here
ADMIN_IDS=123456789,987654321
DATABASE_URL=sqlite+aiosqlite:///./bot.db

# Опциональные параметры
SUPPORT_CHAT=@your_support_username
REFERRAL_COMMISSION=10
ORDER_RESERVATION_MINUTES=15
BROADCAST_THROTTLE=25
NOTIFICATIONS_CHAT_ID=-1001234567890

# Платежные системы (опционально)
YOOKASSA_SHOP_ID=
YOOKASSA_SECRET_KEY=
HELEKET_API_KEY=
```

### Настройка базы данных

#### SQLite (по умолчанию)

Ничего дополнительно настраивать не нужно. База данных создается автоматически.

#### PostgreSQL (для production)

1. Установите PostgreSQL
2. Создайте базу данных:
   ```sql
   CREATE DATABASE tgmailbot_db;
   ```
3. Обновите `DATABASE_URL` в `.env`:
   ```env
   DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/tgmailbot_db
   ```

### Настройка платежных систем

Для пополнения баланса бот поддерживает две платежные системы:
- **ЮКасса** - популярная российская платежная система
- **Heleket** - альтернативная платежная система

Подробные инструкции по настройке каждой платежной системы см. в разделе [Платежные системы](#-платежные-системы).

⚠️ **Важно**: Для работы платежных систем в продакшн необходимо:
1. Настроить webhook для получения уведомлений о платежах
2. Реализовать обработчики webhook в коде (см. раздел [Webhook обработчики](#webhook-обработчики))
3. Настроить SSL сертификат для HTTPS

### Настройка уведомлений

1. Создайте канал в Telegram
2. Добавьте бота в канал как администратора
3. Получите ID канала (используйте [@userinfobot](https://t.me/userinfobot))
4. Добавьте в `.env`:
   ```env
   NOTIFICATIONS_CHAT_ID=-1001234567890
   ```

---

## 💻 Использование

### Первоначальная настройка

#### 1. Создание категории

1. Откройте бота в Telegram
2. Нажмите `⚙️ Пункт управления`
3. Выберите `🛒 Управление каталогом` → `➕ Добавить категорию`
4. Введите название категории

#### 2. Добавление товара

1. `⚙️ Пункт управления` → `🛒 Управление каталогом` → `➕ Добавить товар`
2. Введите название, цену, описание
3. Выберите категорию

#### 3. Загрузка аккаунтов

1. Создайте текстовый файл, где каждая строка - один аккаунт в формате `логин:пароль`
2. `⚙️ Пункт управления` → `🛒 Управление каталогом` → `📥 Импорт товаров`
3. Выберите товар и отправьте файл

### Основные команды

#### Для пользователей

- `/start` - Запуск бота и регистрация
- `📂 Каталог` - Просмотр товаров
- `💰 Баланс` - Просмотр и пополнение баланса
- `📦 Мои заказы` - История заказов
- `👥 Пригласить друга` - Реферальная ссылка
- `💬 Поддержка` - Связь с поддержкой
- `ℹ️ Информация` - FAQ
- `📜 Правила` - Пользовательское соглашение

#### Для администраторов

- `⚙️ Пункт управления` - Админ-панель
  - Управление товарами и категориями
  - Просмотр и управление заказами
  - Управление пользователями
  - Статистика
  - Рассылка
  - Логи ошибок

---

## 🚀 Развертывание

> 📖 **Подробная инструкция по развертыванию в продакшене:** см. [DEPLOY.md](DEPLOY.md)

### Локальный запуск

**⚠️ ВАЖНО:** Запускайте бота через интерпретатор Python из venv:

```bash
# Активируем виртуальное окружение
source venv/bin/activate

# Запуск бота через интерпретатор из venv
venv/bin/python main.py
```

Или если venv активирован:

```bash
python main.py
```

### Развертывание на VPS

#### Использование systemd

1. Создайте файл `/etc/systemd/system/tgmailbot.service`:

```ini
[Unit]
Description=Telegram Mail Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/tgmailbot
Environment="PATH=/path/to/tgmailbot/venv/bin:/usr/bin:/usr/local/bin"
# Запуск бота через интерпретатор из venv
ExecStart=/path/to/tgmailbot/venv/bin/python /path/to/tgmailbot/main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

**⚠️ ВАЖНО:** Убедитесь, что путь к интерпретатору Python из venv указан правильно: `/path/to/tgmailbot/venv/bin/python`

2. Активируйте сервис:

```bash
sudo systemctl enable tgmailbot
sudo systemctl start tgmailbot
sudo systemctl status tgmailbot
```

#### Использование supervisor

1. Установите supervisor:
```bash
sudo apt-get install supervisor
```

2. Создайте конфигурацию `/etc/supervisor/conf.d/tgmailbot.conf`:

```ini
[program:tgmailbot]
command=/path/to/tgmailbot/venv/bin/python /path/to/tgmailbot/main.py
directory=/path/to/tgmailbot
user=your_user
autostart=true
autorestart=true
stderr_logfile=/var/log/tgmailbot.err.log
stdout_logfile=/var/log/tgmailbot.out.log
```

**⚠️ ВАЖНО:** Убедитесь, что путь к интерпретатору Python из venv указан правильно: `/path/to/tgmailbot/venv/bin/python`

3. Перезагрузите supervisor:

```bash
sudo supervisorctl reread
sudo supervisorctl update
sudo supervisorctl start tgmailbot
```

### Webhook режим (для production)

Для production рекомендуется использовать webhook вместо polling. Бот автоматически переключается в webhook режим, если установлена переменная `WEBHOOK_URL`.

#### Настройка webhook для Telegram

1. **Настройте переменные окружения в `.env`:**
   ```env
   # Webhook для Telegram (обязательно для production)
   # Для домена bot.cryptoshop.pro:
   WEBHOOK_URL=https://bot.cryptoshop.pro/webhook/telegram
   
   # Webhook сервер для платежных систем (работает на том же порту)
   PAYMENT_WEBHOOK_PORT=8443
   PAYMENT_WEBHOOK_USE_HTTPS=False
   # SSL сертификаты не нужны, так как Nginx обрабатывает HTTPS
   PAYMENT_WEBHOOK_SSL_CERT_PATH=
   PAYMENT_WEBHOOK_SSL_KEY_PATH=
   ```

2. **Настройте Nginx для проксирования webhook:**
   
   Для домена **bot.cryptoshop.pro** используйте готовую конфигурацию из файла `nginx.conf` в проекте.
   
   Или добавьте в конфигурацию Nginx:
   ```nginx
   # Telegram webhook
   location /webhook/telegram {
       proxy_pass http://127.0.0.1:8443;
       proxy_http_version 1.1;
       proxy_set_header Host $host;
       proxy_set_header X-Real-IP $remote_addr;
       proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
       proxy_set_header X-Forwarded-Proto $scheme;
   }
   
   # Webhook для платежных систем
   location /webhook/yookassa {
       proxy_pass http://127.0.0.1:8443;
       proxy_http_version 1.1;
       proxy_set_header Host $host;
       proxy_set_header X-Real-IP $remote_addr;
       proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
       proxy_set_header X-Forwarded-Proto $scheme;
   }
   
   location /webhook/heleket {
       proxy_pass http://127.0.0.1:8443;
       proxy_http_version 1.1;
       proxy_set_header Host $host;
       proxy_set_header X-Real-IP $remote_addr;
       proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
       proxy_set_header X-Forwarded-Proto $scheme;
   }
   ```

3. **Проверка работы:**
   - Бот автоматически установит webhook при запуске
   - Проверьте логи: `sudo journalctl -u tgmailbot -f`
   - Должно быть сообщение: "Webhook set to https://bot.cryptoshop.pro/webhook/telegram"
   - Проверьте статус webhook: используйте API Telegram или бота @BotFather
   - Проверьте health check: `curl https://bot.cryptoshop.pro/health`

#### Режимы работы

- **Polling режим** (по умолчанию, если `WEBHOOK_URL` не установлен):
  - Используется для разработки
  - Бот сам запрашивает обновления у Telegram
  - Не требует настройки Nginx
  
- **Webhook режим** (если `WEBHOOK_URL` установлен):
  - Используется для production
  - Telegram отправляет обновления на ваш сервер
  - Требует настройки Nginx и SSL сертификата
  - Более эффективен и надежен для production

### Webhook обработчики для платежных систем

Для автоматического пополнения баланса после оплаты необходимо настроить webhook обработчики.

#### Настройка webhook для ЮКасса

1. **В личном кабинете ЮКасса:**
   - Перейдите в настройки магазина → Webhook
   - Добавьте URL: `https://bot.cryptoshop.pro/webhook/yookassa`
   - Выберите события: `payment.succeeded`, `payment.canceled`
   - Сохраните

2. **В коде бота:**
   - Обработчик webhook должен быть реализован в `handlers/payment.py`
   - Проверяйте подпись запросов от ЮКасса для безопасности
   - После успешной оплаты автоматически пополняйте баланс пользователя

#### Настройка webhook для Heleket

1. **В личном кабинете Heleket:**
   - Перейдите в настройки проекта → Webhook
   - Добавьте URL: `https://bot.cryptoshop.pro/webhook/heleket`
   - Выберите события: `payment.success`, `payment.failed`
   - Сохраните

2. **В коде бота:**
   - Обработчик webhook должен быть реализован в `handlers/payment.py`
   - Проверяйте подпись запросов от Heleket для безопасности
   - После успешной оплаты автоматически пополняйте баланс пользователя

#### Важные замечания по безопасности

- ⚠️ **Всегда проверяйте подпись запросов** от платежных систем
- ⚠️ **Используйте HTTPS** для webhook URL
- ⚠️ **Логируйте все webhook запросы** для отладки
- ⚠️ **Обрабатывайте дубликаты** - платежные системы могут отправлять одно событие несколько раз
- ⚠️ **Идемпотентность** - обработка одного платежа должна быть безопасной при повторных запросах

---

## 📁 Структура проекта

```
tgmailbot/
├── main.py                 # Точка входа
├── config.py              # Конфигурация
├── requirements.txt       # Зависимости
├── .env                   # Переменные окружения (создать)
│
├── database/              # База данных
│   ├── __init__.py
│   ├── models.py          # Модели БД
│   └── database.py        # Подключение к БД
│
├── handlers/              # Обработчики команд
│   ├── __init__.py
│   ├── start.py           # /start
│   ├── catalog.py         # Каталог
│   ├── orders.py          # Заказы
│   ├── balance.py         # Баланс
│   ├── payment.py         # Платежи
│   ├── referral.py        # Реферальная система
│   ├── info.py            # Информация
│   ├── admin.py           # Админ-панель
│   └── broadcast.py       # Рассылка
│
├── services/              # Бизнес-логика
│   ├── __init__.py
│   ├── payment.py         # Платежные системы
│   ├── account_service.py # Выдача аккаунтов
│   ├── discount.py        # Расчет скидок
│   ├── notifications.py   # Уведомления
│   └── promotions.py      # Промоакции
│
└── utils/                 # Утилиты
    ├── __init__.py
    ├── keyboards.py       # Клавиатуры
    ├── text.py            # Тексты сообщений
    └── logger.py          # Логирование
```

---

## 💳 Платежные системы

### Пополнение баланса

Бот поддерживает пополнение баланса через следующие платежные системы:

### Оплата заказов

Бот поддерживает оплату заказов через следующие способы:

- **Оплата с баланса** - работает сразу после настройки
- **ЮКасса** - если настроена в `.env`
- **Heleket** - если настроена в `.env`
- **Telegram Stars** - встроенная оплата через Telegram

### Пополнение баланса

Для пополнения баланса доступны только:
- **ЮКасса** - если настроена в `.env`
- **Heleket** - если настроена в `.env`

Если ни одна платежная система не настроена, пользователям будет показана кнопка "Через администратора".

### 💳 ЮКасса (YooKassa)

#### Регистрация и настройка

1. **Регистрация в ЮКасса**
   - Перейдите на [yookassa.ru](https://yookassa.ru/)
   - Зарегистрируйте аккаунт или войдите в существующий
   - Пройдите верификацию (подтверждение личности и банковских реквизитов)

2. **Создание магазина**
   - В личном кабинете перейдите в раздел "Настройки" → "Магазины"
   - Нажмите "Создать магазин"
   - Заполните данные магазина (название, описание, контакты)
   - Сохраните магазин

3. **Получение ключей API**
   - В настройках магазина перейдите в раздел "API"
   - Скопируйте **Shop ID** (идентификатор магазина)
   - Создайте новый ключ API или используйте существующий
   - Скопируйте **Secret Key** (секретный ключ)
   - ⚠️ **Важно**: Secret Key показывается только один раз! Сохраните его сразу.

4. **Настройка Webhook (для продакшн)**
   - В настройках магазина перейдите в раздел "Webhook"
   - Добавьте URL для уведомлений: `https://yourdomain.com/webhook/yookassa`
   - Выберите события для отправки:
     - ✅ `payment.succeeded` - успешная оплата
     - ✅ `payment.canceled` - отмена оплаты
   - Сохраните настройки

5. **Настройка в боте**
   - Добавьте в файл `.env`:
     ```env
     YOOKASSA_SHOP_ID=your_shop_id_here
     YOOKASSA_SECRET_KEY=your_secret_key_here
     ```
   - Перезапустите бота

6. **Проверка работы**
   - Попробуйте пополнить баланс через бота
   - Проверьте логи на наличие ошибок
   - В тестовом режиме используйте тестовые карты ЮКасса

#### Тестовые карты для проверки

- **Успешная оплата**: `5555 5555 5555 4444`
- **Отклоненная оплата**: `5555 5555 5555 4477`
- Срок действия: любая будущая дата (например, 12/25)
- CVV: любые 3 цифры
- Имя держателя: любое

#### Важные замечания

- ⚠️ В тестовом режиме реальные деньги не списываются
- ⚠️ Для продакшн необходимо пройти полную верификацию
- ⚠️ Комиссия ЮКасса: от 2.9% + 15₽ за транзакцию
- ⚠️ Минимальная сумма платежа: 1₽
- ⚠️ Максимальная сумма: зависит от лимитов вашего аккаунта
  

### 💳 Heleket

#### Регистрация и настройка

1. **Регистрация в Heleket**
   - Перейдите на [heleket.com](https://heleket.com/)
   - Зарегистрируйте аккаунт или войдите в существующий
   - Пройдите верификацию (подтверждение личности и банковских реквизитов)

2. **Создание проекта**
   - В личном кабинете перейдите в раздел "Проекты"
   - Нажмите "Создать проект"
   - Заполните данные проекта (название, описание, сайт)
   - Сохраните проект

3. **Получение API ключа**
   - В настройках проекта перейдите в раздел "API"
   - Создайте новый API ключ или используйте существующий
   - Скопируйте **API Key**
   - ⚠️ **Важно**: API Key показывается только один раз! Сохраните его сразу.

4. **Настройка Webhook (для продакшн)**
   - В настройках проекта перейдите в раздел "Webhook"
   - Добавьте URL для уведомлений: `https://yourdomain.com/webhook/heleket`
   - Выберите события для отправки:
     - ✅ `payment.success` - успешная оплата
     - ✅ `payment.failed` - неудачная оплата
   - Сохраните настройки

5. **Настройка в боте**
   - Добавьте в файл `.env`:
     ```env
     HELEKET_API_KEY=your_api_key_here
     ```
   - Перезапустите бота

6. **Проверка работы**
   - Попробуйте пополнить баланс через бота
   - Проверьте логи на наличие ошибок
   - В тестовом режиме используйте тестовые карты Heleket

#### Тестовые карты для проверки

- **Успешная оплата**: `4111 1111 1111 1111`
- **Отклоненная оплата**: `4000 0000 0000 0002`
- Срок действия: любая будущая дата (например, 12/25)
- CVV: любые 3 цифры
- Имя держателя: любое

#### Важные замечания

- ⚠️ В тестовом режиме реальные деньги не списываются
- ⚠️ Для продакшн необходимо пройти полную верификацию
- ⚠️ Комиссия Heleket: уточняйте на сайте
- ⚠️ Минимальная сумма платежа: 1₽
- ⚠️ Максимальная сумма: зависит от лимитов вашего аккаунта

### ⭐ Telegram Stars (только для оплаты заказов)

- Не требует регистрации в сторонних системах
- Используется токен бота
- Включите в [@BotFather](https://t.me/BotFather): `Payments` → `Enable Stars`
- ⚠️ **Важно**: Telegram Stars доступен только для оплаты заказов, не для пополнения баланса

---


## Дополнительная информация

- **База данных**: Создается автоматически при первом запуске
- **Логи**: Сохраняются в `logs/bot.log`
- **Резервное копирование**: Рекомендуется регулярно делать бэкапы БД

---

## 🔧 Технологии

- **Python 3.10+** - Язык программирования
- **aiogram 3.3.0** - Асинхронный фреймворк для Telegram Bot API
- **SQLAlchemy** - ORM для работы с БД
- **aiohttp** - HTTP клиент для платежных систем
- **SQLite/PostgreSQL** - База данных

---

## 🛠️ Требования к серверу

- **Python**: 3.10 или выше
- **RAM**: Минимум 512MB
- **Диск**: Зависит от объема данных
- **Интернет**: Стабильное соединение
- **PostgreSQL**: Опционально, для production

---

## 🐛 Устранение неполадок

### Бот не запускается

1. Проверьте токен бота в `.env`
2. Убедитесь, что все зависимости установлены
3. Проверьте логи: `tail -f logs/bot.log`

### Ошибки базы данных

1. Убедитесь, что файл БД доступен для записи
2. Проверьте права доступа к директории
3. Для PostgreSQL проверьте подключение

### Платежи не работают

1. Проверьте ключи платежных систем в `.env`
2. Убедитесь, что webhook настроен правильно
3. Проверьте логи на наличие ошибок

---

## 📝 Лицензия

Проект создан для внутреннего использования.

---

## 💬 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте логи в `logs/bot.log`
2. Убедитесь, что все переменные окружения настроены правильно
3. Проверьте документацию выше

---

**Версия**: 1.0  
**Последнее обновление**: 2026-01-25

---

<div align="center">

**Сделано с ❤️ для автоматизации продаж**

</div>
